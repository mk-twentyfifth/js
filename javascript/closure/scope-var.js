if (Math.random() > 0.5) {
  var x = "small";
} else {
  var x = "big";
}

console.log(x);

// This can be tricky, because blocks with curly braces do not create scopes, unlike other languages (e.g., C, Java) where blocks create scopes